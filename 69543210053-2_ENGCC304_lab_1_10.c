#include <stdio.h>

int main() {
    int a, b, c;
    int sum;

    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);

    sum = a + b + c;

    printf("Answer = %d\n", sum);

    return 0;
}